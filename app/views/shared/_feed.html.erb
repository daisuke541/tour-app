<% if @feed_items.any? %>
  <ol class="posts">
    <%= render @feed_items %>
  </ol>
  <% post.tags.each do |tag| %>
    <%= tag.name %>
  <% end %>
  <%= will_paginate @feed_items %>
  
  
  
<% end %>
