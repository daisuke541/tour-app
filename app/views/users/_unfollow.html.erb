<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
             <div class ="followbutton">
               <%= f.submit "フォロー解除", class: "button" %>
             </div>
<% end %>
