<%= form_for(current_user.active_relationships.build, remote:true) do |f| %>

  <div class="followbutton">
  <%= hidden_field_tag :followed_id, @user.id %>
  <%= f.submit "フォロー", class: "button" %>
  </div>
  <% end %>
